import { writable } from 'svelte/store';

export const availableSlots = writable([]);

export const disabledSlots = writable([]);

export const allChallenges = writable([]);

export const myChallenges = writable([]);
